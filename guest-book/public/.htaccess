RewriteEngine On

# VERY IMPORTANT: This must match your full base path
RewriteBase /guest-book/public/

# If the file or folder does not physically exist,
# send the request to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
